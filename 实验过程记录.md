# 实验过程记录

## 迁移阶段

### 1. 为什么使用Decoder来重建任务效果变差

- Decoder重建任务和迁移目标不一致：

MMD的目的是使目标域和源域的编码特征分布一致，方便用预训练的Predictor做预测。但是Decoder是基于目标域编码特征去重建输入信号，但这个重建目标并不能保证会让编码的特征空间更好的对齐源域，反而编码器学习了只对重建输入有效的特征，削弱了MMD对齐的效果。

### . **对抗性判别器（Adversarial Discriminator）**

- 类似 Domain-Adversarial Neural Network（DANN）框架。
- 在编码器后添加一个 domain classifier 判别器，让判别器区分“源域 or 目标域”。
- 同时，训练编码器让它“欺骗”判别器（即通过反向梯度或 gradient reversal layer）。
- **目标：让源/目标编码结果难以区分**。

📌 **可以与 MMD 一起使用**，MMD拉近均值差，DANN消除判别信息。

------

### 2. **CORAL（Correlation Alignment）**

- 对齐特征的协方差矩阵：

  LCORAL=∥CS−CT∥F2\mathcal{L}_{\text{CORAL}} = \| C_S - C_T \|_F^2LCORAL=∥CS−CT∥F2

- CSC_SCS、CTC_TCT 是源域、目标域的特征协方差矩阵。

📌 **MMD 对均值有效，CORAL 对协方差有效**，两者互补。

------

### 3. **中心损失 Center Loss / Triplet Loss / Contrastive Loss**

- 从样本级别对齐源/目标特征。
- 可用于学习一个共同嵌入空间。

📌 更适用于目标域有标签的少样本情况（semi-supervised）。

------

### 4. **Reconstruction-based Loss（重建损失）**

- 使用目标域的 `decoder` 重建原始输入（你已经尝试过）。
- 问题在于它没有强约束分布对齐方向。

📌 如果使用，应搭配 MMD / DANN 共同优化，**防止 decoder 过拟合目标域特性**而忽略对齐目标。

------

### 5. **Contrastive Learning（对比学习）**

- InfoNCE，SimCLR，BYOL 等。
- 可以构造目标/源域之间的“正负对”样本，拉近相似特征，推远不同特征。

📌 更适合有标签或伪标签的目标域
